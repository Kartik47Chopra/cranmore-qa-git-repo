# Placeholder defaults so the app boots before secrets are provided.
# Real secret values are delivered via /run/base44/app.env (LAST env_file entry) and override these.
EMERGENT_LLM_KEY=placeholder-not-set
